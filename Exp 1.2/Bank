<!DOCTYPE html>
<html>
<head>
<title>Bank UI LOL</title>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="container mt-5">
    <div class="text-center">
        <h2>Welcome to Bank Bro 💰</h2>
        <img src="bank.png" alt="Bank" style="width:250px;">
    </div>
    <div class="row mt-4">
        <div class="col">
            <input type="number" id="amount" class="form-control" placeholder="Kitna Paisa?">
        </div>
    </div>
    <div class="row mt-3">
        <div class="col-6">
            <button onclick="deposit()" class="btn btn-success w-100">Deposit</button>
        </div>
        <div class="col-6">
            <button onclick="withdraw()" class="btn btn-danger w-100">Withdraw</button>
        </div>
    </div>
    <div class="mt-4 text-center">
        <h4>Balance: ₹ <span id="result">0.00</span></h4>
    </div>
</div>

<script>
let balance = 0; // Initial balance bhai

function deposit(){
    let amt = parseFloat(document.getElementById("amount").value);
    if(isNaN(amt) || amt <= 0){
        alert("Paisa toh daal bhai 😒");
        return;
    }
    balance += amt;
    document.getElementById("result").innerText = balance.toFixed(2);
    alert("Deposited ₹" + amt);
    document.getElementById("amount").value = "";
}

function withdraw(){
    let amt = parseFloat(document.getElementById("amount").value);
    if(isNaN(amt) || amt <= 0){
        alert("Kitna nikaalega bhai? 🤨");
        return;
    }
    if(amt > balance){
        alert("Gareeb! Itna paise nai hai. 😭");
        return;
    }
    balance -= amt;
    document.getElementById("result").innerText = balance.toFixed(2);
    alert("Withdrawn ₹" + amt);
    document.getElementById("amount").value = "";
}
</script>
</body>
</html>
